name: Release Plugin

on:
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: write

jobs:
  build:
    uses: ./.github/workflows/build-and-upload.yml

  release:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@v6
      - name: Download dist artifact
        uses: actions/download-artifact@v8
        with:
          name: plugin-dist
      - name: Prepare plugin zip
        run: |
          mkdir obsidian-pi-plugin
          mv main.js manifest.json styles.css obsidian-pi-plugin/
          zip -r obsidian-pi-plugin.zip obsidian-pi-plugin
      - name: Create Release
        uses: softprops/action-gh-release@v3
        with:
          generate_release_notes: true
          files: obsidian-pi-plugin.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}